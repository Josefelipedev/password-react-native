import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputer: {
        backgroundColor: 'white',
        width: '100%',
        color: 'black',
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        borderRadius: 10,
        borderColor: '#f7b40a',
      textAlign: 'center',
    }
    
});